# ─────────────────────────────────────────────────────────────
# Airco Talks — environment variables
# Copy this file to `.env` and fill in real values.
# NEVER commit `.env`. All keys below are SERVER-ONLY secrets.
# ─────────────────────────────────────────────────────────────

# Sarvam AI — used for STT (Saaras v3 realtime) AND TTS (Bulbul v3).
# Auth header: api-subscription-key
SARVAM_API_KEY=

# Cerebras Inference — LLM (gpt-oss-120b). OpenAI-compatible.
# Auth header: Authorization: Bearer <key>
CEREBRAS_API_KEY=

# ── Server ───────────────────────────────────────────────────
# Port the backend WebSocket server listens on.
PORT=8080
# CORS origins allowed for the browser (comma-separated for multiple, e.g.
# production + preview domains).
WEB_ORIGIN=http://localhost:3000
# Log level: debug | info | warn | error
LOG_LEVEL=info

# ── Voice pipeline tuning ────────────────────────────────────
# Maximum number of recent messages kept as conversation context.
MAX_CONTEXT_MESSAGES=12
# Minimum confidence for a detected language to be surfaced to the UI (0..1).
LANGUAGE_CONFIDENCE_THRESHOLD=0.6
# Audio sample rate (Hz) sent to Sarvam STT.
STT_SAMPLE_RATE=16000
# Silence (ms) after which a locked conversation turn auto-releases to open floor.
TURN_TIMEOUT_MS=12000
# Abort the LLM stream when Cerebras stalls (no token for this long, ms).
LLM_TIMEOUT_MS=15000
# Abort TTS synthesis when Sarvam stalls (no audio for this long, ms).
TTS_TIMEOUT_MS=15000
# TTS preferred voice (Sarvam Bulbul speaker name). Empty = provider default.
TTS_VOICE=
